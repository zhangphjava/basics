
server:
  #端口号
  port: 8082
  servlet:
    encoding:
      charset: UTF-8
      force: true
      enabled: true


spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/basic?useUnicode=true&characterEncoding=utf8&allowMultiQueries=true&serverTimezone=GMT%2B8&useSSL=false
    username: root
    password: root

mybatis:
  mapper-locations: classpath:mapperXml/*.xml